export default function SignInPage() {
  return (
    <main style={{ maxWidth: 480, margin: '0 auto', padding: '4rem 1.5rem' }}>
      <h1>Sign in</h1>
      <p style={{ color: '#9aa3ad' }}>
        Auth UI ships in M1 once Better-Auth is wired against the API.
      </p>
    </main>
  );
}
